name: coffeeshift

services:
  web:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: coffeeshift-web
    restart: unless-stopped
    ports:
      # Override with APP_PORT=... docker compose up -d
      - "${APP_PORT:-4180}:80"
